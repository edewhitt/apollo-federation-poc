sandbox:
  enabled: true

# Sandbox uses introspection to obtain your router's schema.
supergraph:
  introspection: true

# Sandbox requires the default landing page to be disabled.
homepage:
  enabled: false

override_subgraph_url:
  appointments: http://localhost:2121
  providers: http://localhost:2122
  users: http://localhost:2123
